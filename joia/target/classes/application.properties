spring.application.name=joia

# --- Configura��o do MariaDB ---
# Modifique a URL para apontar para o seu banco de dados na porta 3304.
# Crie um banco de dados chamado 'joiadb' no seu MariaDB.
spring.datasource.url=jdbc:mariadb://localhost:3304/joiadb
spring.datasource.username=root
spring.datasource.password=root

# --- Configura��o do JPA/Hibernate para MariaDB ---
# Dialeto espec�fico para MariaDB
spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect
# 'create' apaga e recria o banco de dados a cada inicializa��o.
# Use 'update' se n�o quiser perder os dados ao reiniciar a aplica��o.
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# --- Configura��o de Inicializa��o SQL ---
# Mantido para carregar dados de um poss�vel data.sql, se existir.
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

# --- Configura��o do JWT (JSON Web Token) ---
app.jwtSecret=Yg7eXq9mDzB5kV0Nw8tJRuLp3sZaWcX2QfLhErMjTiVyHbAoGxCdSnUvMtKjPlOi

# Tempo de expira��o do token em milissegundos (24 horas)
app.jwtExpirationMs=86400000